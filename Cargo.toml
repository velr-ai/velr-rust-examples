[package]
name = "velr-consumer"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.8"
velr = { version = "0.1", features = ["arrow-ipc"] }
arrow2 = { version = "0.18", default-features = false }

